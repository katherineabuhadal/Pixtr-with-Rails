<!DOCTYPE html>
<html>
<head>
  <title>Pixtr</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>

<div id="container">
	<h1>pixtr</h1>
	<h4><em>it's the coolest image sharing app everrr</em></h4>
	<% if signed_in? %>
		<%= button_to "Sign out", :session, method: :delete %>
		<div>
    <%= link_to "create a new group", [:new, :group] %>
  </div>
  <% else %>
		<% unless controller_name == "users" && action_name == "new" %>
		<% end %>
		<%= link_to "Sign up", [:new, :user] %>
		<%= link_to "Sign in", [:new, :session] %>
	<% end %>

<%= yield %>
</div>i
</body>
</html>
